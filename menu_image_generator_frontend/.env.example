# Copy this file to `.env` at the project root of the Flutter app and set your OpenAI API key.
# Never commit real secrets to source control.
#
# On first run:
#   cp .env.example .env
#   <edit .env and add your API key>
#
# After updating .env, restart your running app so flutter_dotenv reloads it.

OPENAI_API_KEY=
